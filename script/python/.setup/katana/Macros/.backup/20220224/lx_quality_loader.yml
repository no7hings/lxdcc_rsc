#
input_ports:
    - input
#
output_ports:
    - output
#
parameters:
    reset:
        widget: button
        value: ''
        tool_tip: 'press to rest all parameters'

    quality/scheme:
      widget: enumerate
      value: ['low', 'med', 'hi']
      tool_tip: 'press to rest all parameters'
