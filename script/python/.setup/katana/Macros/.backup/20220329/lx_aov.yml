#
input_ports:
    - input
#
output_ports:
    - output
#
parameters:
    #
    arnold/name:
        widget: string
        value: 'primary'
        tool_tip: '...'

    arnold/channel:
        widget: enumerate
        value: ['rgba', 'primary']
        tool_tip: '...'

    arnold/type:
        widget: enumerate
        value: ['file', 'local']
        tool_tip: '...'

    arnold/file:
        widget: file
        value: '/temp/<layer>/<quality>/<look-pass>/<light-pass>/<camera>/<render-pass>.####.exr'
        tool_tip: '...'